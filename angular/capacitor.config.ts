import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fireside.chat',
  appName: 'fireside-chat',
  webDir: 'dist/fireside-angular'
};

export default config;
